import TaskManager from './TaskManager';
// Assuming you created TaskManager.jsx in the src/ directory

function App() {
  return (
    // Renders the main TaskManager component, which handles all CRUD, filtering, and sorting logic.
    <TaskManager />
  );
}

export default App;